apiVersion: v1
kind: Service
metadata:
  name: nodejs-monitoring-demoapp-service
  labels:
    app: app-deployment
spec:
  type: NodePort
  selector:
    app: app-deployment
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
      nodePort: 32000

